"""
cell.py
    The data structure for the cells.
"""
class Cell:
    """
    Variables:
        idx - unique idx to identify the edge
        edges - list of edges connecting the boundary (are they ordered?)
    """

    def __init__(self, idx, edges)
        self.idx   = idx
        self.edges = edges

